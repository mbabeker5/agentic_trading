## DecisionDesign review (2026-09-06)
Blocking bugs in code: record_fill at agent/loop.py:911 builds Position without stop or target (both 0.0), exit_reason_for skips zero stops, gr.stop_price_for (guardrails.py:1982) is never called from the loop, so in the live path neither the hard stop nor the target fires; only trailing stop and VWAP fade act. A model "fade" answer is a no-op (do_manage acts only on "exit"). Prompt says two closes below VWAP, code fires on one. Prompt tells the model to weight headlines that the scanner never supplies; headline and news keys exist in MOMENTUM_CANDIDATE_KEYS so an unvetted feed would flow straight in. No temperature, top_p or seed pinned; response_format is json_object not strict schema; no confidence field; prompt_hash covers system prompt only. timeout_s defaults to 300 s so one hung call eats the tick. Book B (rules only) picks 3 names (max_picks missing from yaml) while A and E may pick 5, and B holds on every manage tick, confounding the comparison. _clean_pick coerces unknown side to long. No tests for decide.py or models.py.
Changes: 1 set Position.stop and target from the trigger and clamp through stop_price_for so the model can only tighten HIGH BLOCKING; 2 act on fade or remove it HIGH; 3 one VWAP rule rendered from yaml into prompt and code HIGH; 4 drop headline/news keys until a provenance feed exists, then pass source, published_at, first 120 chars inside an untrusted_text fence HIGH; 5 temperature 0, top_p 1, seed, logged per call HIGH; 6 strict json_schema output with confidence and explicit no_action, reject on failure with a decision_rejected ledger row HIGH; 7 hash prompt + packet schema version + output schema + resolved model id HIGH; 8 timeout 45 s, one retry, 60 s budget, fall through to rules tagged model_unavailable, never swap models HIGH; 9 max_picks 5 in both yamls, log picks against the shared shortlist for a paired test HIGH; 10 reject unknown side HIGH; 11 tests/test_decide.py property-style HIGH; 12 nightly replay of a fixed packet set as a drift detector MED. Weekly rationale audit: does the one-liner cite a number actually in the packet.

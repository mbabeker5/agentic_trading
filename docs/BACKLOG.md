# Backlog

Improvements ranked by how much risk they remove, highest first. Operational items ship the same day they are built and tested. Strategy or parameter items wait for Mo's approval through the hub and are stamped into the run registry when applied.

| Rank | Item | Risk it removes | Type | Status |
|---|---|---|---|---|
| 0 | Loop follow-on from the replay gate: fill ingestion from broker executions into book state (`0590a0c`), no re-send while an order is working (`f840f0f`), the bracket's working orders cancelled at the 15:45 flatten (`0a2edc5`), alerts from the loop (`ec49be0`), a Gateway outage handled without a false halt (`ecd34f8`), halts clearable with a bounded reason (`c482c91`), orphan positions alerted rather than halted (`58dfa67`, since overturned by the hub ruling of 2026-09-07 in item 0b: an orphan now halts every book), IBKR 10197 and delayed data visible (`6166471`), symbol exclusivity back to refusing by default for Mo to decide (`055f289`). The sector, halt and cross-book fields were done earlier (`7937e78`) | Gate found nine bugs; no book could trade until fixed | operational | **SHIPPED 2026-09-06. The gate passes 13 of 13, exit code 0, against `c5c92a2`.** Re-stamped to `b04298a` on books A, B and E, which is the last commit in the follow-on batch that moves a limit. A thirteenth scenario, `nothing_left_working`, was added after the item closed: a real bracket goes out in the morning and nothing of that book's can still fill by the close. Four things from these passes are NOT in this item and were opened below: item 14 (`orders.decision_id`), item 15 (the four unmeasured summary columns), item 16 (the flatten's stale duplicate check) and item 17 (the three launchd jobs nothing can generate). Items 14 and 16 shipped on 2026-09-06 as `9f7c04b` and `51da883`, and item 17 shipped on 2026-09-07 as `7449442`; 15 is still open |
| 0a | Books sharing a strategy (A, B, E) collided under one-ticker-one-book | Eval comparison between A, B and E is meaningless if only A ever gets the top names | decision | DECIDED by the hub 2026-09-06: shared symbols are allowed, attribution is by order reference, and reconciliation now compares the broker's net position per symbol against the sum across books. Mo can overturn it; see journal/2026-09-06.md |
| 0b | Does a position no book claims halt every book, or alert and halt nobody? | A holding nobody can account for means either a book has lost its record or somebody traded the account by hand, and every book is then sizing its next order against a picture that is not true. Against that, this account really holds one unclaimed share of SPY from the 2026-09-02 manual test, so halting on an orphan halts all five books on every tick of every day, and a rule that fires every five minutes forever is noise with a halt attached | decision | **DECIDED by the hub 2026-09-07: halt every book.** Shipped as `38912d9`: an unclaimed position puts every book in `books_to_halt`, the loop halts them for the tick (exits still allowed) and sends one alert for the finding, rate limited once per name per day; the `phantom_position` gate scenario asserts it. The only exception is `output/expected_orphans.json`, which must name the symbol and exact quantity; a missing or unreadable file forgives nothing. Live file holds `{"SPY": 1}`, confirmed by a live position read on 2026-09-07 after the Gateway restart. An untagged working ORDER still halts nobody, because a working order changes what nobody holds. Stamp moved on books A, B and E. See `/Users/mtalib/workspace_repos/personal_repo/agentic_trading/config/README_expected_orphans.md` and `journal/2026-09-07.md` |
| 1 | Replay harness as the promotion gate (full five-book loop against recorded data with a fake broker) | Shipping a loop that breaks on its first live day | operational | shipped b5571ca; first run 6 pass 6 fail, see item 0 |
| 2 | Reconciliation on every tick with per-book halt | Trading a book against the wrong picture of what it holds | operational | shipped (reconcile.py, loop) |
| 3 | Watchdog with Gateway restart and Mo alert | Silent multi-day outage, seen 2026-09-03 to 09-06 | operational | shipped, launchd job not yet loaded |
| 4 | Pre-flight at 9:00 with no-trade-today file | Trading on delayed data or an unreconciled book | operational | shipped, launchd job not yet loaded |
| 5 | Kill switch: cancel all, flatten all, disable loop | No fast way to stop a runaway book | operational | shipped 9e095d6 plus dead-man b151446 |
| 6 | Per-book day-trade counter, hard limit on books C and D | Live-money account lock under the pattern day trader rule | operational | building |
| 6a | Day-trade rules by regime: read the account's day-trades-remaining on Tuesday, make the counter and hard limit conditional on the old pattern day trader regime, assert no order can create an intraday margin deficit under the new regime (FINRA Notice 26-10) | Enforcing a retired rule, or missing the new one, on live money | operational | building |
| 6b | Watchdog verifies a restart by new pid and fresh login time, not by the start script's exit code | A dead Gateway reported as restarted | operational | shipped c0d29f9 |
| 7 | Portability: single project root, generated launchd, MIGRATION.md | Cannot move to the Mac Mini without a rewrite | operational | shipped |
| 8 | Fill quality tracking: decided price versus fill price per book | Paper results that hide slippage | operational | ledger columns shipped c2e995a; loop passes decision price in the Momentum v2 pass |
| 9 | Holiday calendar in guardrails | Loop treats a market holiday as a trading day | operational | shipped f8e719a |
| 10 | Consolidated quote feed ($10 IBKR bundle) | Pricing limit orders off a fifth of the market | data, Mo's call | proposed |
| 11 | Second free data source for Congress trades | Single volunteer-run mirror going stale | data | proposed |
| 12 | Go live ramp (Mo's decision D4, 2026-09-06): 10 percent of intended size for 10 sessions, then 25, then 50, then full, with an automatic drop back to paper on a cap breach, an unreconciled position, a crash holding a position, two daily cap hits in five sessions, drawdown over 8 percent, or live slippage above twice paper | Going live at full size on a machine that has never handled real money | operational | ladder and demotion triggers written into config/books.yaml as `live_ramp` and a per book `ramp_step`; the code that enforces the session count and the demotion triggers is NOT built |
| 13 | IBKR order precautions (Mo's decision D6, 2026-09-06): never bypass them for API orders, set each just above the matching cap in config/guardrails.yaml, and raise a precaution rejection as an alert rather than retrying it | One layer of caps instead of two | operational | decision recorded in config/books.yaml; the Gateway settings themselves are set when a live account is first funded |
| 14 | `orders.decision_id` is never set. `record_order_row` in `agent/loop.py` does not pass it, so `db.trades_for_date`'s join to `decisions` finds nothing | Every trade row the nightly Sheet sync writes comes out with a blank model, cost, prompt hash and rationale, so a month of results cannot be read against the model that produced it or the price it cost | operational | **SHIPPED 2026-09-06 (`9f7c04b`).** `BookTick.record` hands back the decisions row id. The dry run path already wrote the judgement first, so it passes the id down. The live path writes the judgement, then `submit()` writes the order row BEFORE it sends rather than after, so the row can carry the link, and the broker's own order id, its one-cancels-the-other group and the final status are written in from the answer a moment later through `db.update_order_status`, which now leaves out of the UPDATE anything it was not given. Writing the order row first is the safer order too: an order that reached IBKR and then lost this process is on the record instead of missing from it. One gap left on purpose and NOT part of this item: a market order that comes back already filled never becomes a working order, so nothing carries its `db_order_id` and its fill lands with no order behind it. That cannot happen against the replay broker, which fills on the next bar, and it wants a lookup by the broker's own order id to close properly |
| 15 | Four columns of `daily_book_summaries` are NULL because nothing measures them: `spy_close`, `max_drawdown_pct`, `rule_triggers` and `missed_ticks` | The month end scoreboard is the point of the table, and three of the four are exactly the per book figures the Google Sheet cannot produce | operational | open, and left NULL on purpose in the meantime. NULL means not measured; a zero would mean measured and none. `rule_triggers` is a `COUNT` over the `decisions` table, where every guardrail firing already sits with its rule id; `missed_ticks` needs an expected tick count, which nothing produces yet |
| 16 | The flatten's duplicate check reads a list taken before its own cancels. `main()` in `agent/loop.py` reads the account's working orders once per tick, so the closing order at 15:45 is refused by `duplicate_order` naming the stop the same tick has already cancelled | The position is closed on the next look instead, five minutes later in the gate and thirty seconds later in production, so nothing is left open overnight. What it costs is that the flatten is one tick slower than it reads, and on a fast close that is real | operational | **SHIPPED 2026-09-06 (`51da883`).** Found by the `nothing_left_working` scenario rather than by reading the code. The once a tick broker read stays exactly as it was, because five books each asking IB Gateway the same question in the same second is how a data pacing violation happens and the fifth book's call really did time out at 45 seconds. Instead a cancel drops the order it pulled out of the list this tick already holds: `BookTick.forget_order`, called from all three places that cancel, the flatten, the stop move and the market backstop. THE MARKET BACKSTOP WAS BROKEN BY THE SAME BUG and now works: it cancels a triggered stop-limit and sends a market order on the same side in the same name, and that market order was being refused every time. `nothing_left_working` now fails on a `duplicate_order` refusal from 15:45 on instead of noting it, and checks the cancels and the closing order land on one tick. Two smaller things found on the way and NOT fixed here: `market_out_unfilled_stops` never calls `day_trade_check`, so on books C and D the backstop can complete a round trip the pattern day trader limit refused (today it is stopped by the resting target instead, which is luck), and `ORDER_FAMILY` in `agent/loop.py` has no `target` key, so a resting target is classed as an opening order by the book's own half of the duplicate check |
| 17 | Three launchd jobs could not be generated: `sheet_sync`, `backup_db` and `deadman`. The last is the dead man's handle, the only job in this project whose purpose is to place an order, so `output/heartbeat` had no reader on a schedule | A loop that dies holding a position stays dead holding it, and the nightly Sheet sync and the database backup never run | operational | **SHIPPED 2026-09-07.** All three are ordinary templates now and all three are loaded, so `launchctl list` shows nine `com.mtalib.agentic-trading` jobs. The diagnosis in `journal/hub_status.md` line 8 was wrong and worth keeping on the record: it said the cause was a file extension and that renaming the three or widening the glob would fix it. The glob was widened in `6fc3fd3` and it did not fix it, because the three files were whole finished plists with `{ROOT}` written through them, made by hand before the generator existed, with no schedule in English to expand. A rename would not have worked either. The real fix was to rewrite each one as `config/launchd/templates/<job>.template`, add its wake up count to `EXPECTED_WAKE_UPS` in `tests/test_paths.py`, and delete the hand made file. Each was run once by hand first: the sheet sync against the live Google Sheet with the result read back through the Sheets API, the backup to a 352K file, and the dead man's handle as a dry run only. Mo approved the kill switch and unattended operation on 2026-09-06 and the hub ruled on 2026-09-07. One thing found on the way and NOT fixed: this Mac is set to Pacific, not Eastern, so all nine jobs currently fire three hours late against New York. That is one system setting, not a code change, and it is written up in `docs/LAUNCHD.md` and `docs/OPERATIONS.md` |
| 18 | A test run can leave a state file for a FUTURE date that the loop then loads as real. Five files `output/state_BOOK_A_2026-09-08.json` to `state_BOOK_E_2026-09-08.json` were written by a test at 13:22 on 2026-09-06 under rules `3b06b34`, each with `picked_at` set. `load_state` in `agent/book_state.py` loads a file by date if it exists, and `phase_for` in `agent/loop.py` treats a set `picked_at` as the pick already made | On Tuesday 2026-09-08, the first trading day, every book would skip its first real pick. Found by the learning loop on 2026-09-07, see `journal/2026-09-07.md` | operational | **SHIPPED 2026-09-07 (`53c69d9`).** The ten files were moved out of `output/` by hand first. Two independent fixes landed, either of which would have been enough. (a) A tick whose `--now` DATE is not today's real date writes everything to `output/rehearsal/<today's real date>/`: state, packets, heartbeat, `next_tick_seconds` and the tick log. Redirected rather than refused behind a flag, because a rehearsal is a useful thing the loop's own docstring recommends, and a flag is one more thing to remember at 13:22 on a Saturday when the whole problem is that nobody remembered. `paths.output_dir()` now honours `AGENTIC_TRADING_OUTPUT_DIR`, which the loop sets for the length of one tick and restores afterwards, so the scanner and the sweeps, which run as their own processes, move with it. The three kill switch files are still read from the real `output/`, so a rehearsal is stopped by a real `STOP` and cannot write one a real tick would obey; and a root that is already a copy, which is every test and the replay harness, is left alone. (b) `BookState` gains `created_at`, stamped from the REAL clock and never from the tick's pretend one. `last_tick` could not do this job: a rehearsal writes its pretend time there, so Saturday's file says Tuesday. When `load_state` opens a file for a day that has already begun and it was written before that day began, the file is moved to `output/stale/` and the book starts the day fresh, carrying forward what the previous day ended holding. Files with no `created_at`, which is all five real ones, fall back to the file's modification time. Two conditions on purpose: a file written today for tomorrow is early, not wrong, and a replay of a day gone by is not wrong at all. 19 tests in `tests/test_stale_state.py` |
| 19 | Reconciliation prints "everything matched" when the broker could not be read at all. On 2026-09-07 it did so on the three ticks (07:37, 09:40, 09:48) where the account showed as worth 0.00 with 0 positions because the reads failed | The one line a reader trusts most says a comparison happened when nothing was compared, and the orphan halt cannot fire on such a tick | operational | **SHIPPED 2026-09-07 (`7801267`).** The line now reads `not checked: the broker could not be read (positions timed out, open orders timed out)` and the orphan check is reported as not run, because a holding nobody claims cannot be spotted on a tick where the holdings never arrived. `books_agree` stays true and nobody is halted, exactly as on the day: an unreadable broker is not a book being wrong about its own holdings, and halting five books over a Gateway hiccup is the failure `ecd34f8` exists to prevent. `BrokerFacts` now writes down every read that did not come back, by name and in words, and `ReconcileOutcome` carries `checked`. Found on the way and worth knowing: the MCP client's own wording, "took longer than 45 seconds to answer tools/call", matches none of `OUTAGE_PHRASES`, which is why those three ticks were never read as an outage in the first place. Whether it should be is a separate question and deliberately not answered here, because making it one would blind the whole tick rather than reword one line, and a blinded tick manages no positions |
| 21 | The watchdog cannot tell a Mac with no internet from a dead Gateway. On 2026-09-08 from about 12:33 to 12:55 the Mac's network was down (the watchdog's own Slack sends failed on a DNS lookup), the real read check timed out, and the watchdog stopped a healthy Gateway and started a new one that then sat on "connection error, will retry" until the network returned at 12:55. It also reported "restart did not take", which was true and unfixable from the Mac | A network blip becomes a 19 minute Gateway logout on top of the blip, four missed ticks, and a dead man's handle firing; on a live day with positions that is 19 minutes with no stop moved. Found by the learning loop, see `journal/2026-09-08.md` | operational | open. Sketch: before `ib_answers` blames the Gateway, resolve IBKR's host name or fetch a known page with a short deadline; if that fails, alert "the Mac has no internet, leaving the Gateway alone" and skip the restart. Needs a test that fakes the DNS failure |
| 22 | Alerts that fail to reach Slack are lost for good. Four watchdog alerts between 12:36 and 12:50 on 2026-09-08, including "restart did not take", reached the Mac's screen only because DNS was down, and nothing retried them when the network came back at 12:55 | Mo reads Slack and sees a quiet day where the most interesting thing of the day happened; the record of an outage lives only on a screen nobody was watching | operational | open, and now urgent rather than small. **2026-09-09 made it five times worse: 20 of that day's 50 alerts never reached Slack**, failing continuously from 03:00:56 to 11:40:05 on the same name lookup error, and they included the pre-flight failure, "restart did not take" and every warning about an eight hour Gateway outage. Mo's Slack was silent from 03:00 to 12:15 on the most eventful morning the project has had. The pattern is now clear and is the reason to fix this: one network fault takes out the Gateway and the alert channel together, so the days worth hearing about are exactly the days nothing gets through. Sketch unchanged: `agent/alerts.py` writes a failed Slack send to `output/alerts_pending.jsonl` and the next successful send, or the next watchdog run, flushes it with the original stamp |
| 23 | The watchdog will not restart a Gateway whose process is up but whose port is shut, and nothing bounds how long it waits. `_may_restart` in `agent/watchdog.py` line 529 only lets a port failure trigger a restart when `gateway_process` is also failing. On 2026-09-09 a wedged Gateway held process 57216 from 03:01 to 12:20 having never completed a login, so the port stayed shut and the process check kept passing. The watchdog raised "gateway_port is still failing" at 04:00, 05:00, 06:00, 07:00, 08:00, 09:25, 09:55, 10:30, 11:05, 11:40 and 12:15 and attempted no repair at any of them. What finally freed it was the wedged process dying on its own at about 12:20, which made `gateway_process` fail newly and earned a restart; the Gateway was back 68 seconds later | 8 hours 20 minutes of a trading day, the entire morning session, on a fault the watchdog already knew how to fix. It is the single largest measured cost in the project so far. Found by the learning loop, see `journal/2026-09-09.md` | operational | open, and the highest priority item on this list. Sketch: keep the existing rule for the first few minutes, since a shut port on a fresh process really is just a slow start, then allow a stop-then-start once `failing_since` on `gateway_port` is older than about 15 minutes. The safe machinery already exists: `restart_gateway(stop_first=True)` is what `ib_answers` uses, and stopping first is exactly what removes the "two logins fighting over one session" risk the current comment is guarding against. Note the second half of the trap while fixing it: `ib_answers` is the only check allowed to stop and restart, and it has to connect through the port to notice anything, so on a shut port it is skipped and can never fire. Needs a test that fakes a process that is up with a shut port for 20 minutes |
| 24 | The two sweeps sit behind a Gateway check they do not need. `agent/run_tick.sh` line 85 tests port 4002 and exits 1 before Python ever runs, so book D's 07:30 Congress sweep and book C's 16:30 insider sweep die with the tick. Neither `agent/sweep_congress.py` nor `agent/sweep_insider.py` contains a single reference to `ib_async`, port 4002, `IB()` or the MCP server; they read the House and Senate disclosure sites and EDGAR over ordinary HTTP | On 2026-09-09 book D had no shortlist all day and goes into the next morning with none either, purely because the broker was down at 07:30. Book C's evening sweep survived only because the Gateway happened to be back by 16:30; on a day the Gateway is also down in the evening, both books lose the next morning too. Found by the learning loop, see `journal/2026-09-09.md` | operational | open. Sketch: run the sweep phase before the port check in `run_tick.sh`, or give the two sweeps their own launchd jobs on their own schedules. The second is cleaner but adds two more jobs to `EXPECTED_WAKE_UPS` in `tests/test_paths.py`. Either way the sweeps must keep writing to the same shortlist paths the books already read |
| 25 | The delayed feed's volume field is nonsense and `MAX_PLAUSIBLE_VOLUME` is set about a thousand times too high to catch it. The ceiling in `agent/replay/record_day.py` line 181 is 100 billion shares, chosen on 2026-09-06 on the belief that the nonsense values are astronomically large (delayed SPY came back with 34,054,225,730,979 shares). On 2026-09-09 the values arrived in a range that straddles the ceiling: 16 of 18 symbols were caught and voided, and 22 readings passed, the largest being 96,844,470,371 shares of JLHL at 12:40 and the smallest 9,646,308,876 shares of OABI at 16:05. The whole US market trades roughly 10 to 15 billion shares on a normal day | A recording is the input to the replay gate, which is the promotion gate for every strategy change. 22 fake liquidity readings are in `output/recordings/2026-09-09/snapshots.jsonl` right now, and anything that sizes a position or judges liquidity off them is being fed a number a thousand times too large. Found by the learning loop, see `journal/2026-09-09.md` | operational | open. Sketch, two parts. (a) Prefer the volume on the five minute historical bar, which is sane in the same recording on the same day: SPY's 15:45 bar reads 572,657 shares. The bars are already collected, so this is a change of which field is trusted, not new data. (b) Drop the ceiling to about 10 billion. Also correct the comment above it, which says this only happens "when the market is shut": on 2026-09-09 it happened from 12:25 through 15:55 with the market open |
| 26 | The `loop_tick` watchdog check reads the last tick time out of `output/state_BOOK_A_<date>.json`, which only a tick that got past the Gateway ever writes. A loop waking on schedule and exiting early at the port check therefore looks exactly like a loop that has died | On 2026-09-09 it alerted "The trading loop last ticked 1019 minutes ago" from 09:30 and repeated it until 12:15, while the loop was in fact firing every five minutes exactly as scheduled and had done 64 times. The alert is not merely useless during an outage, it sends whoever reads it to check launchd and the tick log for a problem that is not there, while the real fault sits one check away. Found by the learning loop, see `journal/2026-09-09.md` | operational | open. Sketch: read the modification time of `output/tick_<date>.log`, which a Gateway-down tick still appends to, or have `run_tick.sh` touch a separate "the loop woke up" file before it checks port 4002 and leave `output/heartbeat` meaning what it means now, which is "a tick completed". Then distinguish the two states in the alert text: "the loop is running and cannot reach the Gateway" against "the loop has stopped waking up" |
| 27 | The MCP server stopped surviving between ticks on 2026-09-09 and nothing in the repository explains why. All 45 working ticks that day logged "MCP server is not answering. Starting it with agent/start_mcp.sh" and got a fresh process id; across 106 ticks on 2026-09-07 and 2026-09-08 that line appears zero times and every tick found it already answering. No code changed between those days, the last commit being `819a095`. `agent/run_tick.sh` does not stop it, yet `output/mcp_logs/mcp_ibkr.log` records 46 shutdowns that day | Nothing broke, and 2 to 4 seconds a tick is affordable. What changed is the shape of the dependency: reaching the broker at all now requires starting a server successfully every five minutes rather than once, and a tick that cannot start one is a tick with no broker. There is also a live loose end: process 56318, started by the 16:30 tick, took its stop signal at 16:32:50, logged "Waiting for connections to close" and never logged a finish, the only one of 46 that day to do so, and it was still running an hour later holding port 8765 and client id 100 | operational | open, and the smallest item here. Sketch: read `output/mcp_logs/mcp_ibkr.log` around the first tick of 2026-09-09 to find what ended the long-lived server, then decide deliberately whether the MCP server is a long-lived service or a per-tick child, and make `run_tick.sh` say so either way. Whichever is chosen, a shutdown that hangs on an open connection needs a bounded wait and then a kill |
| 20 | `output/alerts.log` mixes time zones: one hand written line is stamped 07:00 PDT between lines stamped EDT | Anyone reading the file in order, or a script sorting it, puts that alert three hours out of place | operational | **SHIPPED 2026-09-07 (`5a65b42`).** `append_log` went through `datetime.now().astimezone()`, so an alert took the zone of whatever shell sent it. It now goes through `alerts.log_stamp()`, which converts to `America/New_York` whatever `TZ` says, matching `agent/db.py`'s `as_ts` and every other log in the project. Alerts written by hand still go through `agent/alerts.py --level ... --title ... --body ...`, which is now written down in `docs/OPERATIONS.md` |

## Research gaps (from the momentum spec critique, 2026-09-06)

| Rank | Item | Why it matters | Type | Status |
|---|---|---|---|---|
| R1 | Second literature pass on opening-range results: the published work traces to one research group with no independent replication or post-2023 out-of-sample test | The strategy's edge rests on it | research | open |
| R2 | Rerun the Congress-trades research with sources that are not script-rendered or rate-limited before any of its figures are used | Unverified numbers in the spec | research | open |
| R3 | Check commission and fee figures in the execution review against IBKR's live schedule | Cost model may be off | research | open |
| R4 | Confirm on Tuesday 2026-09-08 whether scanner filters and real-time quotes reach the paper account after the subscription purchase | Everything downstream depends on it | research | checked 2026-09-08: NO. Morning blocked by a competing login (10197); afternoon, after a fresh Gateway login at 12:55, IBKR said "not subscribed to real time data for the API" (10089, 10168) on every check. Scanner returned zero rows all day so filters were untestable. Next: Mo checks the subscription and paper sharing in Client Portal, then re-check Wednesday 09:25. See `journal/2026-09-08.md`. **Re-checked 2026-09-09 and now settled.** The competing session is genuinely gone: zero occurrences of 10197 anywhere that day against 1,758 the day before. What is left is the real answer, and it is still no: IBKR returned 10089 and 10168, "requires additional subscription for API", 807 times between 12:25 and 16:05. Delayed data does now reach the account, 810 snapshots all carrying a price with SPY's bid and ask a cent apart, which is new. Filters are still untestable because the Gateway was down until 12:25 and no scan ran. It is now Mo's action in Client Portal, not a research question. See `journal/2026-09-09.md` |

## Dated reminders

Things that are not due yet and must not be forgotten. Each one has a date it
becomes live, and nothing here is actionable before that date.

| Due | Item | Why the date | Status |
|---|---|---|---|
| 2027-03-01 | **Section 475(f) election, mark to market accounting.** Talk to a trader tax CPA about whether to make the election for the 2027 tax year, and file it if the answer is yes (Mo's decision D8, 2026-09-06). | The election has to be filed by the due date of the prior year's return, WITHOUT extensions, and it CANNOT be made retroactively. Miss the date and the earliest it can apply is the year after. 1 March leaves a working month before the usual mid-April deadline, which is enough time to find a CPA who does this and to change your mind. | not due yet |

Two facts to hand the CPA, both from the momentum spec critique of 2026-09-06:
short term gains are taxed as ordinary income, and wash sale rules bite across
the year end, which is exactly what a day trading book generates. The election
is the thing that turns both of those off, at the cost of giving up capital gain
treatment entirely. It is a real decision with real downside, not a formality,
which is why it gets a CPA and not a search engine.
